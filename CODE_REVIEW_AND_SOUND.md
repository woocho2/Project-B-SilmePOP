# 코드 검토 및 사운드 사용 안내

## 구조

- `UIManager_Start` → 시작 연출 → `SceneLoader` → 메뉴/게임 씬 이동.
- `GameManager`는 제한 시간, 일시정지, 재시작, 게임오버를 관리합니다.
- `SlimeManager`는 기본 17×10 보드 생성, 직사각형 합 10 탐색, 힌트/섞기/단일 파괴를 담당합니다.
- `DragManager`는 마우스 드래그와 점수를, `DragBoxArea`는 겹친 슬라임과 합 판정을 담당합니다.
- `SlimeController`는 숫자, 색 대비, 하이라이트, 콜라이더를 관리합니다.
- `UIManager_Menu`, `UIManager_Game`은 버튼과 커스터마이징을 연결합니다. `ChangeData`는 씬 사이 선택값을 유지하고 `Change`는 변경 이벤트를 받아 외형을 적용합니다. `ThemeData`는 맵 스프라이트 에셋입니다.
- `FlexibleColorPicker`는 외부 색상 선택기입니다. 이번 수정 범위에서는 외부 에셋을 변경하지 않았습니다.

## 반영한 개선

- 매칭 탐색에 재사용하는 2차원 누적합을 적용했습니다. 사각형 내부를 매번 순회하던 계산이 네 번의 배열 조회로 바뀝니다. 최악 시간복잡도는 열 C, 행 R에 대해 O(C³R³)에서 O(C²R²)로 줄어듭니다. 기존 탐색 순서와 첫 힌트 결과는 유지합니다.
- `Change`는 자기 파트에 필요한 이벤트만 구독합니다. 첫 구독을 Start로 옮겨 다른 매니저의 Awake보다 먼저 실행되는 문제를 피하고, 구독했던 인스턴스로 해제합니다.
- 일시정지 중 타이머 감소, 재시작 후 이전 지연 게임오버 실행, 게임오버 중복 실행을 방지합니다. 타이머 UI는 실제 남은 시간으로 표시합니다.
- 드래그 매니저가 꺼지거나 파괴 스킬을 사용할 때 드래그를 취소합니다. 씬 종료 시 별도로 생성한 드래그 박스를 정리합니다.
- 재시작 시 이전 슬라임을 즉시 비활성화해 Destroy가 처리될 때까지 새 보드와 겹치는 것을 방지합니다.
- 커스텀 적용 버튼과 홈 버튼의 누락 참조 검사, 점수/게임오버 이벤트 구독 해제를 보완했습니다.
- 수정한 기존 스크립트는 한글을 보존하여 UTF-8로 저장했습니다.

## 사운드 연결

1. Unity Project 창에서 `Assets/Resources/SoundData.asset`을 선택합니다.
2. `Background Music`에는 기존 `Assets/Sound/Slime Splash Quest.mp3`가 연결되어 있습니다. 게임 실행 시 자동 재생되고 씬 전환에도 유지됩니다. 씬에 SoundManager를 따로 붙일 필요가 없습니다.
3. `Click`, `Match`, `Miss`, `Hint`, `Mix`, `Destroy`, `Game Over`에 원하는 AudioClip을 드래그합니다. 각 게임 동작의 재생 호출은 연결되어 있습니다. 빈 칸은 조용히 건너뜁니다. 현재 저장소에는 BGM만 있어 효과음 칸은 비어 있습니다.
4. 개별 오브젝트에는 `SoundPlayer`를 추가하고 `Clip`을 넣습니다. Button On Click 또는 다른 UnityEvent에 `SoundPlayer.Play()`를 연결합니다. Clip이 없으면 선택한 Effect의 공통 클립을 사용합니다. `Play On Enable`을 켜면 오브젝트 활성화 때 재생합니다. 기존 UI 매니저가 관리하는 버튼에는 공통 클릭음이 이미 연결되어 있어 중복 연결하지 않습니다.
5. 음량 Slider의 범위를 0~1로 설정하고 `SoundPlayer.SetMusicVolume(float)` 또는 `SetEffectsVolume(float)`의 동적 이벤트를 연결할 수 있습니다. 음량은 PlayerPrefs에 저장됩니다. 저장된 값이 설정 에셋의 기본 음량보다 우선합니다.

`SoundManager`는 BGM용 AudioSource 1개와 효과음용 12개를 재사용합니다. 효과음이 모두 재생 중이면 다음 순서의 음원을 교체하여 동시 재생 수를 제한합니다. `PlayMusic(clip)`과 `StopMusic()`도 코드에서 사용할 수 있습니다.

## 후속 개선 후보

- 메뉴/게임의 커스터마이징 버튼 바인딩이 크게 중복됩니다. 공통 컴포넌트로 추출하면 변경 누락을 줄일 수 있습니다. 씬 참조를 함께 이전해야 하므로 별도 작업으로 권장합니다.
- 슬라임 170개를 재시작마다 Instantiate/Destroy합니다. 실제 Profiler에서 재시작 스파이크가 확인되면 풀링을 적용할 수 있습니다. 숫자·힌트·콜라이더 상태 초기화도 함께 설계해야 합니다.
- 드래그 영역의 Trigger 결과는 물리 업데이트 주기에 영향을 받습니다. 빠르게 드래그하고 즉시 놓을 때 마지막 영역을 누락하는지 플레이 검증이 필요합니다.
- `Camera.main` 반복 접근, 섞기 List/선택 배열 할당은 작은 최적화 후보입니다. 매칭 탐색보다 우선순위는 낮습니다.
- `ChangeData`는 메모리에만 저장하므로 앱을 종료하면 선택이 초기화됩니다. 영구 저장이 필요하다면 에셋 ID 기반 저장 구조가 필요합니다.
- 메뉴 종료 버튼은 기존 코드에 종료 동작이 연결되어 있지 않습니다.

## 검증

- Unity 6000.3.9f1 프로젝트 참조를 사용한 전체 C# 컴파일: 오류 0, 외부 FlexibleColorPicker의 기존 미할당 필드 경고 4개.
- 실제 수정된 TryFindMatch 메서드를 추출하여 난수 시드 461의 2,000개 보드를 완전 탐색과 비교: 매치 존재 여부 및 첫 사각형 좌표 모두 일치. 빈 보드, null/비활성 셀, 크기 변경 및 17×10 보드 포함.
- 코드 diff 공백 검사 통과.
- Unity Play Mode에서 실제 청취 및 씬 전환/일시정지/재시작 통합 검증은 수행하지 않았습니다. BGM 유지, 효과음 중첩, 옵션 음량, 드래그 중 정지, 지연 게임오버 대기 중 재시작을 확인하면 됩니다.
